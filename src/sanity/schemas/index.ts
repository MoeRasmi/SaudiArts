import { artwork } from './artwork'
import { category } from './category'
import { siteSettings } from './siteSettings'

export const schemas = [artwork, category, siteSettings]

export { artwork, category, siteSettings }
